# v0.62.0

## ⚙️ Escrita de preferências com isolamento PROVADO — o tema desta versão

- **Guard real de isolamento do `definir_pref`** (#1593): o teste que faltava — provar que a preferência de um usuário nunca vaza pro balde de outro — agora existe e **morde** (mutante do balde fixo derruba o teste; re-gate da QA contra a pre-prod mesclada). O card refez o fluxo completo por ordem do PO antes de fechar: dev → review → gate → aprovação, sem atalho.
- **Detetor de Definição-de-Ready no board** (#1642): o `board.ps1` agora flaga cards Ready incompletos — o requisito do canon vira mecanismo que não esquece, em vez de lembrança de quem faz grooming.

## Também nesta versão

- **Remote — aviso de expiração da credencial TURN** (#1148 fatia C): a UI passa a avisar quando a credencial efêmera está a expirar (só avisa; renovação é fatia seguinte). Corrigido também o overclaim de skew no teste de TTL.
- **TURN — a outra ponta da travessia** (#1130-b): `turn_relay_echo`, o peer externo do probe — pronto pra prova de travessia real com IP público.
- **Roster JSON com guarda de escrita** (#1654 fatia 1): escrita tudo-ou-nada por papel + gate de CI; mensagens de erro nomeiam o suspeito.
- **galaxie.db** (#1655): base SQLite portátil de histórico/métricas + wrapper `galaxie-db.ps1` (o card fecha no fluxo do board; o código embarca aqui).
- **Cofre DPAPI dos client_secret OAuth** (#1549): `scripts/galaxie-oauth.ps1` — o PO registra os segredos localmente com digitação oculta; caveat local-vs-prod documentado (produção usa Docker secrets `_FILE` na VPS).
- **Canon v1.20–v1.21**: emenda de canon vai a PO Approved direto; exceção de despacho do SM para fila parada (decreto verificável).
