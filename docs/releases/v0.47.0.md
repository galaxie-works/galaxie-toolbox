## 📬 Bridge — leitor de e-mail
- **Corpo do e-mail volta a aparecer inteiro**: corrigida a regressão que espremia a mensagem numa faixa de ~120px com scroll interno, e o tratamento de cores do corpo no tema escuro voltou.

## 🔔 Aviso de atualização
- **As notas de versão aparecem de verdade**: texto formatado (sem `##`/`**` crus) e com rolagem quando são longas.
- **Data da versão legível** no selo do aviso, em vez de carimbo de máquina.

## 🧹 App mais enxuto
- **Atoms, Comms e Pulsar foram removidos** (telas, comandos, preferências e textos) — eram recursos invisíveis e sem uso, por decisão de produto. O Astro fica.

## 🛡️ GALAXIE Remote (servidor)
- **Registro de dispositivo com prova de posse da chave**, atrás de flag de servidor (desligada por padrão) — fecha o vetor de sequestro de sessão no cadastro, sem mudar o comportamento até a janela de ativação.
- **Matrícula autorizada por ticket de enrollment** no canal v2 (contrato) — quem entra precisa de um ticket assinado, não basta se declarar.
- **Autorização por-frame do host** passa pelo funil único, com o gate de Input incluído.

## 🔧 Sob o capô
- Pirata (Lottie) da aba privada: estado bom pinado por teste em navegador real.
- Porta determinística `?tela=<id>` para telas ocultas, só em desenvolvimento (QA).
- CI: clippy do remote-signaling verde de novo (atributo reposto).
