## 🖥️ GALAXIE Remote
- **Clique no lugar certo com vídeo letterboxed**: o mapeamento de coordenadas usa a área real do frame, não a viewport com bordas — o clique remoto cai exatamente onde você apontou.

## 📬 Bridge
- **Largura do sidebar em pixels, não em fração da janela**: em monitores largos o sidebar mantém os ~256px de projeto em vez de engordar até ~590px.

## 👥 Pessoas
- **Tela de contatos fatiada e mais leve**: detail, ações em massa e código comum separados — abrir e interagir com contatos ficou mais responsivo.

## 🔧 Sob o capô
- Guarda de fiação do modal de update: ignorar o compare de versões agora derruba a suíte (fecha o buraco do #1264).
- Guarda no `release.yml` pega `$notas` literal (regressão residual do #1270).
- Zerados os 4 warnings Rust que shiparam na v0.52.0; preview de docx amarra `sandbox` + CSP do iframe por teste; pareamento de `fontes_conhecidas()` guardado.
