## 🚀 Apps e navegação
- **Fixar (pin) funciona de verdade**: apps M365/GALAXIE fixados aparecem no rail e no grupo Fixados do comando, e o grupo respeita o termo da busca — Enter abre o que você buscou, não o fixado.
- **Ícones do catálogo consertados**: 210 ícones que eram JPEG/PNG/WEBP renomeados de .svg agora renderizam pela extensão verdadeira, e os 10 que sumiam no tema claro ficaram visíveis.

## 📬 Bridge
- **"Mover para pasta…" rola em vez de esticar** quando a lista de pastas é longa.

## 🖥️ Interface
- **Sino de status vem primeiro** na fileira de ícones da barra de título, com tooltip acessível.

## 🛡️ GALAXIE Remote (servidor)
- **Matrícula autorizada no canal v2 completa**: owner/org saem da sessão verificada (nunca do payload), capabilities concedidas pelo servidor (default-deny), teto de validade do ticket virou invariante de compilação, e revogação no lugar.

## 🔧 Sob o capô
- Infra do VPS versionada no repo (traefik/openobserve/remote), `/healthz` responde o commit no ar e o procedimento de aplicar infra é único — mata a deriva invisível entre repo e servidor.
- Files: guarda de fiação do nome de drive de rede (2º round) e guardas das pastas no conteúdo (#1283, fatias).
- Testes de contatos tipados de verdade (sem cast).
