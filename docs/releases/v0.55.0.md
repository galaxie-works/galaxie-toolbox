## 📬 Bridge
- **As três divisórias do Bridge agora são a mesma coisa**: o splitter entre sidebar e lista ganhou o punho e o hover dos outros dois — arrastar ficou consistente e visível.

## 🛡️ Segurança
- **Pipe SYSTEM com porteiro de comando (default-deny)**: o agente SYSTEM do Remote só executa comandos da lista permitida — qualquer outro é recusado na porta.
- **`turn_secret` fora do `argv`**: o script de verificação do relay não expõe mais o segredo na lista de processos do host (o vetor que forçou a rotação de 19/08).

## 🔧 Sob o capô
- Guarda do `release.yml` por allowlist: a regressão do placeholder de notas não volta nem por variável.
