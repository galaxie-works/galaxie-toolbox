## 📁 Files
- **Raízes com cara própria**: Cloud, Rede e Acesso rápido ganham ícones e títulos de fonte única; cada mount de nuvem mostra o logo do serviço; This PC com ícone próprio.
- **Home view**: o atalho da pasta pessoal vira "Início", com as subpastas da home como tiles.
- **Menu de contexto acionável na árvore do sidebar** e itens do sistema podem ser desafixados do Acesso rápido.

## 📬 Bridge
- **Sidebar para de engordar com a janela**: redimensionar a janela não estica mais o sidebar do splitter.

## 🛡️ GALAXIE Remote (servidor)
- **Credencial TURN efêmera**: o registro no canal v2 entrega `ice_servers` com credencial temporária, e o segredo TURN nunca aparece em log (guardado por teste).

## 🔧 Sob o capô
- Telemetria fail-closed com guarda (a decisão sai do leitor de ambiente).
- Guarda do atalho F validada por AST (não por texto); LottieIcon (código morto) removido; janela declara `unminimize` com catraca de capabilities; runbook com nomes verificados; refactors do leitor (props/paginação/hooks) e QA visual pela porta de dev.
